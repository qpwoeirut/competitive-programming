unit rail;
interface
uses graderhelperlib;

procedure findLocation(n,first : Longint;var location,stype : array of Longint);

implementation

procedure findLocation(n,first : Longint;var location,stype : array of Longint);
begin

end;
	
end.














