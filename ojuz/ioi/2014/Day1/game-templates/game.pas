unit game;

interface

procedure initialize(n: longint);
function hasEdge(u, v: longint): longint;

implementation

procedure initialize(n: longint);
begin
end;

function hasEdge(u, v: longint): longint;
begin
    hasEdge := 0;
end;

end.
