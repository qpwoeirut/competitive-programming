unit boxes;

interface 

function delivery(N, K, L : longint;  p : array of longint) : int64;

implementation

function delivery(N, K, L : longint;  p : array of longint) : int64;
begin
	delivery := 0;
end;
end.
