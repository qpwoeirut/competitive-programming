#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;

typedef long long ll;

const ll MIN_N = 1;
const ll MAX_N = 1e6;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);
    int N = inf.readInt(MIN_N, MAX_N, "n"); // change to readLong if values can be longs
    inf.readEoln();
    inf.readEof();
}

