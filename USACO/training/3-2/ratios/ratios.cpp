/*
ID: zhongbr1
TASK: ratios
LANG: C++14
*/

#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    freopen("ratios.in", "r", stdin);
    freopen("ratios.out", "w", stdout);

    int B, O, W;
    cin >> B >> O >> W;

    int mix[3][3];
    for (int i=0; i<3; i++) {
        for (int j=0; j<3; j++) {
            cin >> mix[i][j];
        }
    }
}
