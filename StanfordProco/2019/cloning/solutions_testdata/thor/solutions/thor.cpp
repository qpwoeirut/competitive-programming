#include <bits/stdc++.h>
using namespace std;
typedef long double ld;
typedef long long ll;
typedef pair<ll,ll> pii;

int main(){
    ios::sync_with_stdio(0);
    ll n,m; cin>>n>>m;
	ll k=(m/n);
	cout<<(k*k*k)<<endl;
    return 0;
}
